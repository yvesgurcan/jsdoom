// see SPRITES.md for details
import { DIRECTIONS } from '../engine/constants';

const {
    NORTH,
    EAST,
    SOUTH,
    WEST,
} = DIRECTIONS;

export default [
	{ type: 'shotgunGuy', x: 12, y: 6, rotDeg: NORTH },
	// { type: 'imp', x: 18, y: 10, rotDeg: EAST },
	// { type: 'baronOfHell', x: 18, y: 11, rotDeg: SOUTH },
];
